# SkillGo v0.4.4

本版本把任务改为**完全无人值守、一口气执行到底**：Agent 不再能够向用户追问。

## 主要更新

**移除 ask_user 追问机制**

- Agent 工具集中删除 `ask_user`：工具 schema、动作参数校验、模型响应解析（独占调用规则）、系统提示中的示例与规则全部同步移除。
- 系统提示新增明确的无人值守规则：缺少关键单位、范围或业务参数时，Agent 必须采用与输入和用户目标最一致的合理假设继续执行，把假设记录下来并在最终摘要中披露；只有输入本身不可用（文件损坏/无法解析）或任何合理解释都无法消除的矛盾，才允许用 `block` 诚实终止。
- 模型若因旧习惯发出 `ask_user`，收到的是一条**可恢复**的工具返回（指引其自行假设并继续）；若反复停留在该动作，由既有的重复动作卡死保护以 `SANDBOX_AGENT_STALLED` 如实失败——任务不会再被挂起等待人工。
- 同时修正此前一条系统规则"用 ask_user 暴露矛盾材料"，改为自行选择最合理解释并在最终摘要说明。

**历史兼容**

- `waiting_user` 状态、任务对象的 `pending_question` 字段、网页回答入口与 `POST /api/v1/jobs/{id}/answer` 均保留，但只为 v0.4.3 之前已经挂起的历史任务服务；新任务不会再进入该状态。
- 外部 API 文档与 Python 示例已更新：外部集成无需实现任何问答回调；示例客户端保留对历史 `waiting_user` 任务的防御性退出。

## 升级

本版本**无数据库迁移、无配置变更**，重建 api、worker 镜像即可（请在无运行中任务时执行）。

```bash
sudo SKILLGO_INSTALL_ROOT=/opt/skillgo \
  SKILLGO_DEPLOY_ENV=deploy/ecs.env \
  bash deploy/upgrade-skillgo.sh v0.4.4
```

## 验证

- 后端全部回归通过；原"追问挂起"端到端测试改写为"重复 ask_user 被可恢复指引纠正、持续停留按卡死保护失败且不产生任何 question 事件"。
