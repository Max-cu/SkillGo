# Phase 0 Echo Skill

This is a deterministic fixed-entry Skill used only by the SkillGo sandbox
proof of concept. It reads a platform-generated JSON contract and writes one
JSON result under `/output`.

It does not require network access or third-party Python packages.
