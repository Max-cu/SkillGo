# 模型等待与执行效率排查（2026-09-08）

## 线上证据

读取 IDA 3005 校对技能的四条历史任务事件，未重新运行这些业务任务。

| 任务前缀 | 已完成模型调用累计秒数 | 工具累计秒数 | 结束原因 |
| --- | ---: | ---: | --- |
| 33a87a8e | 584.40 | 3.21 | 最后模型请求约 500 秒后失败 |
| a482e77b | 551.99 | 3.60 | 最后模型请求约 500 秒后失败 |
| 472b0252 | 529.58 | 3.07 | 最后模型请求约 500 秒后失败 |
| 4ca2c6b7 | 1673.05 | 5.82 | 整次执行达到 1800 秒 |

旧版失败模型事件保持 running 且没有 duration_ms；前三条的末次等待由事件时间差计算。第四条另有约 120 秒未完成的末次请求。上述累计模型耗时不包含未完成请求。失败发生前均未调用最终验证器。

已完成模型轮次中，生成约 36 KB 文件参数的一轮耗时约 487 秒。工具本身通常只需数百毫秒。可见错误包括跨 Python 进程使用未定义变量、空解析结果索引越界及生成代码语法错误。不能将这些时间归因于 gVisor 性能或最终验证。

模型连接原先已配置 500 秒等待，并非默认 120 秒。该 Skill 指南为 65,387 UTF-8 字节，旧上下文投影每轮重复携带一份。当前服务器未找到这个 Skill 的成功历史，无法据此量化与升级前的速度差异。

## 本次调整

- 该 DeepSeek 连接等待由 500 调至 600 秒，保存原值并写入系统审计。保持原模型、推理配置、上下文和输出预算。
- 此部署 `.env` 和 `deploy/ecs.env` 的任务总时限由 1800 调至 3600 秒；不改变仓库的通用默认值。
- 去除已在固定系统提示中完整保留的重复 Skill 正文。多 Skill 活动指南仍按预算保留，原始请求不裁剪。
- 增加独立 Python 进程、可复用解析模块、文件持久化的执行指引，不修改发布后的 Skill 或业务规则。
- 模型错误区分连接、响应超时、传输中断；失败轮次持久化耗时和错误码；记录消息数量和保守输入 token 估算（不含工具定义，非供应商精确 tokenizer）。
- 传输日志仅记录尝试序号、耗时、HTTP 状态或异常类型，不记录 URL、凭据、提示或模型正文。
- 新增短暂传输异常重试类型，仍最多三次且共用原请求总时限，不重放已执行工具，不拦截任务取消。

## 验证与回滚

全量后端回归 216 项通过；随后新增失败模型事件持久化用例，韧性专项 11 项通过。现有 Starlette/httpx 弃用提示仍存在。

线上验证：API/Worker 内四个模块的哈希与部署清单一致；有效模型等待为 600 秒，Worker 总时限为 3600 秒；API 与数据库健康、网页首页及代理健康路由通过。真实 gVisor 自检确认 `runsc`、断网、权限限制、产物读取和临时沙箱回收。使用同一 DeepSeek 配置的合成工具请求约 25 秒返回 HTTP 200 和有效 `list_files` 调用；该探针不执行返回的工具，不读取业务附件。

部署前核对四个变更模块与本地 v0.3.0 的 SHA-256 一致；备份路径为服务器 `/opt/skillgo/.deploy/model-tuning-20260908/`，包含 `before.tar.gz`（源码和原部署配置）、`model-before.json`（原模型非敏感参数）与 `manifest.json`（前后源码哈希）。原镜像保留为 `skillgo-api:before-model-tuning-20260908` 与 `skillgo-worker:before-model-tuning-20260908`。

回滚应在无活动任务时恢复该目录下的源码和配置备份，将目标模型等待恢复为备份值，并使用保留镜像重建 API/Worker；待 API 健康后重建 Web，使 Nginx 更新后端地址。不涉及数据库结构迁移或删除业务记录。

更长等待只是降低过早终止的概率，不保证任务完成。真实校对速度、规则覆盖和文档质量仍须用业务任务验证；本次不把合成连通性检查当成完整业务验收。
